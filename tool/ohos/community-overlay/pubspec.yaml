name: sitemark
description: "Community Flutter overlay for HarmonyOS HAP review only."
publish_to: 'none'
version: 1.0.8+23

environment:
  sdk: '>=3.6.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
