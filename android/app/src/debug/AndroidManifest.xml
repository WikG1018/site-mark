<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- D-023: debug inherits INTERNET + ACCESS_NETWORK_STATE from the
         main manifest so NAS sync can be tested on debug APKs. Flutter's
         default debug INTERNET (hot reload) now matches that permission
         instead of being stripped at merge time. -->
</manifest>
