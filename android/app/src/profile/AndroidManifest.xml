<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- D-023: profile inherits INTERNET + ACCESS_NETWORK_STATE from the
         main manifest. Do not strip INTERNET here — NAS sync must work
         on profile builds the same as release. -->
</manifest>
